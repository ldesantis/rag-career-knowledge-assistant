The first portfolio project is a RAG Career Knowledge Assistant. It will load documents, split them into chunks, create embeddings, store them in a vector database, retrieve relevant context, and generate grounded answers.